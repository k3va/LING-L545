DELIMITERS = ".";

LIST DET = DET;
LIST PUNCT = PUNCT;
LIST NOUN = NOUN;
LIST ADP = ADP;

SECTION

SELECT DET IF (1C NOUN);
SELECT ADP IF (1C DET) (-1C NOUN);
SELECT NOUN IF (-1C DET);
SELECT ADP IF (-1C DET);
